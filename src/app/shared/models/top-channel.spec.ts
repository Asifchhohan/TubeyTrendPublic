describe('top-channel model', () => {
  it('model file compiles (type-only interface)', () => {
    // `TopChannel` is an interface; no runtime instantiation possible.
    expect(true).toBeTrue();
  });
});
